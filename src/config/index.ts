// ============================================
// CONFIGURATION - RE-EXPORT FROM ROOT
// ============================================

// Re-export the root config
export { librarianConfig, uiConfig, type LibrarianConfig } from '../../librarian.config';
